# Home Weather

A live WeatherFlow station dashboard for station `63486`.

Open `index.html` directly in a browser, or serve the repository root and visit `/home-weather/`.

The app is static and fetches the station observation endpoint from the browser.
